Some Random Notes and Thoughts
==============================

MuscatFX \STATLINE... -> $if{$or{$if{$ne{$msize,0},ok},$queryterms},...}

Add ability to lookup in table (e.g. en -> English)?  Or just use create map
like so: $set{Len,English}$set{Lfr,French}$set{Lde,German}
And then lookup like so: $var{L$field{language}}

Wrap up code into objects...

Clean up page description language.

Document page description language.

Sort out some images...

Query parser and indexer parser should share code somehow.

Split long HTML docs at some <A NAME=...> tags.

Boolean filters like `domain:uk' in query... (domain/site/language/host/...)
