#!/bin/sh
set -e

# If we're being purged, then delete the templates directory.
if [ "$1" = purge ] ; then
  rm -rf /var/lib/xapian-omega/templates
fi

# dh_installdeb will replace the magic cookie below with any postrm actions
# generated by other debhelper scripts.

#DEBHELPER#

exit 0
