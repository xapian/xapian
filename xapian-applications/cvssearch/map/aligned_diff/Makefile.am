bin_PROGRAMS = 
noinst_LIBRARIES = libaligned_diff.a
INCDIRS = include diff range alignment process
INCLUDES = $(foreach dir, $(INCDIRS), -I$(srcdir)/../$(dir))
libaligned_diff_a_SOURCES = aligned_diff.cpp aligned_diff.h

