#!/bin/sh
set -e

docdir=/usr/share/doc/@PACKAGE@

if [ ! -L "$docdir" -a -d "$docdir" ] ; then
  rmdir "$docdir"
  ln -s '@TARGET@' "$docdir"
fi

# dh_installdeb will replace the magic cookie below with any postinst actions
# generated by other debhelper scripts.

#DEBHELPER#

exit 0
