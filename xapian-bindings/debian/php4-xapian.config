#!/bin/sh -e

# Major version of PHP we're installing for.
PHP_VERSION=4

# Source the debconf library.
. /usr/share/debconf/confmodule

# Ask the user (unless they've asked not to see medium priority questions).
db_input medium "php$PHP_VERSION-xapian/modify-phpini" || true
db_go || true

exit 0
