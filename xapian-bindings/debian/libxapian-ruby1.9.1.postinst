#!/bin/sh
set -e

docdir=/usr/share/doc/libxapian-ruby1.9.1

if [ ! -L "$docdir" -a -d "$docdir" ] ; then
  rmdir "$docdir"
  ln -s 'ruby-xapian' "$docdir"
fi

# dh_installdeb will replace the magic cookie below with any postinst actions
# generated by other debhelper scripts.

#DEBHELPER#

exit 0
